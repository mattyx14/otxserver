---
name: MySQL Schema Check
on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "schema.sql"
  merge_group:
  push:
    paths:
      - "schema.sql"
    branches:
      - main

jobs:
  mysql-schema-check:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: otxserver
          MYSQL_USER: otxserver
          MYSQL_PASSWORD: otxserver
        ports:
          - 3306/tcp
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3
    strategy:
      fail-fast: false
    name: Check
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.1
      - name: 📌 MySQL Start & init & show db
        run: |
          sudo /etc/init.d/mysql start
          mysql -e 'CREATE DATABASE otxserver;' -uroot -proot
          mysql -e "SHOW DATABASES" -uroot -proot
      - name: Import OTXServer Schema
        run: |
          mysql -uroot -proot otxserver < schema.sql
